CREATE TABLE employee (
  employee_id int(11) NOT NULL AUTO_INCREMENT,
  employee_name varchar(45),
  employee_email varchar(45),
  employee_salary varchar(40),
  employee_leaves varchar(40),
  PRIMARY KEY (employee_id)
);

